[package]
name = "hx711-async"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
critical-section = "1.2.0"
defmt = { version = "1.0.1", optional = true }
embedded-hal = "1.0.0"
embedded-hal-async = "1.0.0"

[features]
defmt = ["dep:defmt"]
